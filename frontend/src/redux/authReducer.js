const initialState = {
  id: 0,
  username: "",
  image: "",
  password: "",
  isLoggedIn: false,
};
const authReducer = (state = initialState, action) => {
  return state;
};

export default authReducer;
